part uuid mmc ${devnum}:2 uuid

setenv bootargs "console=ttyS2,1500000 earlycon=uart8250,mmio32,0xff130000 root=PARTUUID=${uuid} rw rootwait"

load mmc ${devnum}:1 ${pxefile_addr_r} kernel.img

bootm ${pxefile_addr_r}
